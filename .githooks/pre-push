#!/usr/bin/env sh

set -eu

if ! command -v go >/dev/null 2>&1; then
  echo "pre-push: go is required but not found in PATH" >&2
  exit 1
fi

echo "pre-push: running go test ./..."
go test ./...

echo "pre-push: test checks passed"
